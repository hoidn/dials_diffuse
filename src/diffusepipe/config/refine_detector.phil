refinement {
  parameterisation {
    detector {
      fix = all
      hierarchy_level = 0 
    }
    beam {
      fix = all
    }
    crystal {
      fix = cell // Keep cell fixed for now
    }
    scan_varying = False 
  }
  reflections {
    outlier {
      algorithm = null
    }
  }
}
